import type { UserInteraction } from "../../../types/spotCollectionItem";

const BlockCommentSection = ({
  userInteraction,
}: {
  userInteraction: UserInteraction;
}) => {
  return <div>BlockCommentSection</div>;
};

export default BlockCommentSection;
